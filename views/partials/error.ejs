<% if(typeof errors !== 'undefined' && errors && errors.length > 0){ %>
    <div>
        <span>Errors!</span>
        <% errors.forEach(error => { %>
            <li><%= error %></li>
        <% }) %>
    </div>
<% } %>